# 高中化學晶體綜合比較問答遊戲

靜態單頁應用，包含題庫與互動邏輯。

如何使用：

1. 在此資料夾啟動簡單靜態伺服器，例如使用 Python：

```bash
python3 -m http.server 8000
```

2. 打開瀏覽器並前往 `http://localhost:8000/`。

檔案：
- index.html — 主頁面
- style.css — 樣式
- questions.js — 題庫
- app.js — 遊戲邏輯

如需修改題庫或行為，編輯 `questions.js` 或 `app.js`。
